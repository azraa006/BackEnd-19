datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Restaurants {
  rest_id String @unique @default(uuid())
  rest_title String
  rest_address String
  rest_time String
  rest_img_url String
  rest_is_one_free Boolean
}