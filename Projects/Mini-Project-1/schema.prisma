datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Student{
  roll_no String @unique
  name String
  gender String
  dob String
  phone_no String
}